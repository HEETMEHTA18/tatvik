import 'package:flutter/material.dart';

class WorldMonitorWeb extends StatelessWidget {
  const WorldMonitorWeb({super.key});
  
  @override
  Widget build(BuildContext context) {
    return const SizedBox.shrink();
  }
}
