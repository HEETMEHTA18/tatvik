# Task

Add GitHub write-back: commit DevMentor-generated todos to `.autodevs/todo/` on selected repo.

---

## Objective

Close the outbound gap in the voice-to-code supply chain so mobile ideas reach the laptop without manual copy-paste.

---

## Requirements

- New endpoint `POST /api/v1/github/commit-todo` (or extend `github.py`)
- Accept: `repo_full_name`, `todo_markdown`, `branch` (default: main)
- Use user's GitHub OAuth token to create/update file via Contents API
- Filename: `todo_YYYY_MM_DD_<slug>.md`
- Flutter: "Ship to GitHub" action from Prompt Hub after loop generation
- Return commit SHA and file path

---

## Constraints

- Follow existing `github_service.py` patterns
- Do not break read-only `sync-github`
- Require authenticated user
- Write tests with mocked GitHub API

---

## Acceptance Criteria

- Authenticated user can commit a todo to `.autodevs/todo/` on a synced repo
- Duplicate slug gets unique suffix or version number
- `pytest` passes for new endpoint
- No secrets logged

---

## Priority

High

---

Generated By:
DevMentor

Timestamp:
2026-06-10T00:00:00Z
